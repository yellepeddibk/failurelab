name: Evaluator request
description: Propose a new deterministic evaluator metric or analyzer
body:
  - type: input
    id: metric
    attributes:
      label: Evaluator name
    validations:
      required: true
  - type: textarea
    id: behavior
    attributes:
      label: Deterministic behavior and denominator
    validations:
      required: true
